/*
var modulo = require('./declvar.js');
modulo.acao();
*/
/*
Tipagem de variáveis
*/
var numero = 1;  // Tipo Numérico (1, 2.5, 0)
console.log(numero);
console.log(typeof(numero));

var palavra = "JavaScript"; // Tipo String (Cadeia de Caracteres)
console.log(palavra);

var logica = true; // Tipo booleano (Representa Verdadeiro (true) e False (falso) )
console.log(logica);

var qualquer; // sem valor atribuido, sem tipagem
console.log(qualquer);

//console.log(x);
